/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.ibb.co",
      "www.shareicon.net",
      "plus.unsplash.com",
      "images.unsplash.com",
      "cdn.dummyjson.com",
    ],
  },
};

export default nextConfig;
