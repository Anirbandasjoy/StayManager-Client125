import LoadingAnimation from "@/components/loading/UiLoading";
import React from "react";

const loading = () => {
  return (
    <div>
      <LoadingAnimation />
    </div>
  );
};

export default loading;
